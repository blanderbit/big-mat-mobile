import { ActivityIndicator as RNActivityIndicator } from 'react-native';

import { colors } from '@extra/colors';

export const ActivityIndicator = () => {
  return <RNActivityIndicator color={colors.darkOrange} />;
};
